var salario = prompt("Por favor, digite seu salário");
var
for (salario < 1000.00);
{
	document.write(" Isento de impostos");
}
	else (salario >= 1000.00 && salario <= 2500.00) {
	salario1 = (salario - 2000.00) * 12, 5
	document.write("seu imposto é:", imposto1)
}
	else {
	if ( >= 2500 && salario <= 5000.00) {
		imposto2 = 80 + (salario - 3000.00) * 25
		document.write("seu imposto é:", imposto2)
	}
	lse
	{
		imposto3 = 80 + 270 + (salario - 4500.00) * 0.28
		document.write("seu imposto é:", imposto3)

		funcion
	}
}
}
	}


se o salário está entre $1000 e $2500, desconte 12, 5 % do salário
c.se o salário está entre $2500 e $5000, desconte 25 % do salário
d.se o salário for maior que $5000, desconte um valor fixo de $1300.